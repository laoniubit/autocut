# 一刀不剪 Matrix AI - 工业级语义切片与精密视频管线 (v9.0.0)

![Standard](https://img.shields.io/badge/Standard-Industrial_Grade-D70000?style=for-the-badge)
![Design](https://img.shields.io/badge/System-Obsidian_Red_2.0-black?style=for-the-badge)
![AI](https://img.shields.io/badge/AI-Whisper_WebGPU-blue?style=for-the-badge)
![Security](https://img.shields.io/badge/Security-HWID_Locked-green?style=for-the-badge)

**一刀不剪 Matrix AI** 是一款专为高频创作（TikTok/抖音/短视频）设计的**工业级精密视频自动化切片系统**。基于 **Obsidain Red 2.0** 视觉语言，系统提供从 AI 语义检索到物理渲染的全链路闭环解决方案。

---

## 💎 核心引擎特性 (v9.0 Industrial)

- **🧠 纯语义 AI 切片 (Pure Semantic Slicing)**：
  - 完全摒弃传统视觉波动检测，采用 Whisper 离线模型进行全闭环语义分析。
  - **AI Elite Selector**：基于信息密度与叙事逻辑的黄金片段自动筛选算法。
- **🚀 黑色工业管线 (Night-Stream Pipeline)**：
  - **硬件自适应架构**：支持 WebGPU 离线推理与 NVIDIA NVENC 物理分片。
  - **原子化文件系统**：所有配置文件与任务索引采用 `save_json_atomic` 机制，严防意外断电导致的任务损坏。
- **🛡️ 矩阵审计与安全**：
  - **HWID 硬件锁定**：基于设备指纹的工业级授权管理，支持完全断网环境下的“离线握手”校验。
  - **日志审计流**：全量操作实时归档至 `debug.txt`，冷启动异常自动记录至 `STARTUP_ERROR.TXT`。
- **🎨 Obsidian Red 2.0 美学设计**：
  - **沉浸式交互**：深色工业美学配合 9:16 专业预览窗口设计。
  - **实时仪表盘**：动态展示 ASR 引擎状态、算力储备与管线并发进度。

---

## 🛠️ 技术底座

- **核心后端**: Python 3.x, Flask, Socket.IO (多线程异步架构)
- **AI 算力**: Transformers.js / ONNX Runtime (支持 WebGPU 加速)
- **视音频处理**: FFmpeg (内置 2026 稳定版驱动)
- **前端交互**: Vanilla ES6+, CSS 磨砂玻璃 (Obsidian Red 2.0)

---

## 📂 工业级项目结构

```text
autocut/
├── workdir/             # 工业工作区：按项目 ID 管理物理资产
│   └── [ID]/            # 项目容器 (例如: 1, 2, 3...)
│       ├── in/          # 原始素材存放处
│       ├── temp/        # AI 索引、缩略图、原子索引 (JSON)
│       └── out/         # 最终合成的工业标准 MP4
├── models/              # 完全离线 AI 模型权重库
├── static/              # Obsidian Red 2.0 前端资产
├── acut_server.py       # 后端中枢：Socket 通讯与 API 指令集
├── acut_engine.py       # 逻辑引擎：FFmpeg 调度与语义分拣
├── acut_license.py      # 安全中枢：HWID 核发与授权校验
├── debug.txt            # 系统物理审计日志
└── STARTUP_ERROR.TXT    # 关键宕机保护日志 (仅在启动失败时生成)
```

---

## 📦 快速启动流程

1. **运行环境**: 已内置全套 FFmpeg 依赖，解压即用。
2. **启动指令**:
   - 双击 `autocut.exe` (或运行 `python acut_server.py`)。
   - 系统将自动在浏览器中唤起 **Obsidian Red 2.0** 操作界面。
3. **资产导入**: 拖拽视频进入“资产库”，系统将自动发起 ASR 语义对齐。

---

## ⚙️ 工业标准配置 (config.json)

系统核心参数通过原子化 JSON 维持持久化存储：

```json
{
    "output_specs": {
        "width": 1080, "height": 1920,
        "video_bitrate": "5M",
        "export_preset": "ultrafast"
    },
    "ai_specs": {
        "auto_select_limit_s": 120 // 单次切片最大语素跨度
    }
}
```

---

## ⚠️ 权利要求与安全须知

- **HWID 核发**: 本软件依赖硬件指纹（HWID）进行授权。请确保 `acut_license.py` 与本地环境匹配。
- **离线原则**: 系统优先使用 `models/` 目录下的权重。在无网络环境下，AI 功能依然可全量运行。
- **纯英原则**: 为确保 FFMPEG 路径索引的绝对稳健，建议项目根路径位于全英文目录下。

---

## 🟢 页脚状态探测算法 (Footer Probes)

工业级系统的页脚（Footer）不仅仅是版本展示，更是一个**实时物理网络连通性探针（Network Reachability Probe）**。其核心算法原理如下：

- **探测机制**：前端在进行环境校验时，会向关键云端节点发起极其轻量的旁路探测（`fetch` 请求）。
- **参数约束**：严格采用 `mode: 'no-cors'`（无需跨域数据，仅探测 TCP/HTTP 握手成功率）与 `cache: 'no-store'`（强制绕过本地缓存，反映真实的即时网络状态）。
- **状态机反馈**：
  1. **授权中枢连通性 (绿点/红点)**：向 `https://abc.nelsoncode.com` 发起探测，若网络可达，页脚最右侧的 `status-dot` 将亮起绿色；若断网或解析失败，则呈现红色（或灰色），警示离线状态。
  2. **DeepSeek 代理服务器 (文本颜色)**：向 `https://deep.xmbs.top` 发起探测，若连通正常，绿点左侧的 `DeepSeek` 字样将渲染为绿色；反之则为红色。
- **架构意义**：通过被动式的“无痕探针”，在不干预离线剪辑核心工作流的前提下，直观呈现本地机器对于外部关键服务（授权核验、AI大语言模型语义筛选）的网络健康度。

---
*Matrix AI - Precision is not an option, It is the standard.*
