# 🔱 AutoCut Matrix AI 智能视频剪辑系统
## 官方用户帮助手册 (最新保姆级版)

> **核心引擎**：Matrix AI 智能人声语义切片引擎  
> **运行版本**：最新 Matrix AI 生产线 (一键整合版)  
> 💡 **版本说明**：最新版已实现 **CPU 与 GPU（显卡加速）引擎的物理合并**。软件在启动时会自动诊断您的电脑硬件。您可以在软件界面中一键切换 CPU 模式与 GPU 模式，无需下载或安装不同的版本。GPU 模式下处理速度是 CPU 模式的 10-20 倍。

---

## 🚀 一、 软件核心功能 (Software Features)
> [!NOTE]
> **AutoCut Matrix AI** 是一款专为短视频、自媒体创作者打造的**智能视频剪辑软件**。它能将您的视频语音自动转为文字，让您像“改作文”一样，通过点击和勾选字幕快速剪掉无意义的废话、语气词和静音，并能一键将视频转换为人脸自动居中的 9:16 竖屏短视频（系统默认格式）。

### 1. AI 语音转文字与智能声学断句
*   **全自动高精转写**：采用工业级离线语音识别技术，自动将视频中的语音转化为高精度的文本。
*   **智能边界磁吸**：不同于常规的时间轴切片，系统会自动识别语音中的真实停顿，将切片点毫秒级吸附对齐到人声起止点，自动剔除无意义的吸气声，**有效防止剪出的视频出现“吞字”或“破音”**。

### 2. DeepSeek 智能黄金片段筛选
软件深度集成 DeepSeek 大模型，为您提供高维度的“叙事导演级”智能分选：
*   **自定义加/减分项**：在筛选时，您可以填入“加分项”（如：核心卖点、穿搭展示）和“减分项”（如：废话、闲聊、降价促销），AI 会自动对每个片段进行高维评分。
*   **时长额度精算**：设定您期望的视频总时间（例如 90 秒），大模型会自动挑选评分最高的黄金干货片段填入导出队列，帮您完成 80% 的粗剪工作。
*   **智能时序重排**：大模型会按照叙事逻辑（开场勾子 -> 核心爆点 -> 金句总结）自动调整片段顺序，确保叙事一气呵成。

### 3. 极速渲染与自适应画面裁剪
*   **人脸追踪与画面居中 (默认 9:16)**：软件合成时会自动识别并锁定人物脸部，通过平滑的防抖追踪将画面自适应裁切为 9:16 的竖屏短视频黄金比例（系统默认规格）。
*   **高清字幕烧录**：在合成时，自动进行美化换行断句，并在视频底部烧录清晰的粗体字幕。
*   **保留原声与静音**：可自由选择保留音频或导出无声视频，便于后期做二次音效处理。

---

## 🛠 二、 安装与部署手册 (Installation Manual)

### 1. 软件部署
1.  **解压**：将下载的压缩包整体解压到本地文件夹。
    > [!IMPORTANT]
    > **避坑指南**：解压后的文件夹路径中**绝对不能包含中文、空格或特殊字符**！
    > * ❌ 错误路径：`D:\我的软件\视频剪辑\autocut\`
    > *  正确路径：`D:\autocut\`
2.  **启动**：双击运行主程序 `autocut.exe` (或 `AutoCut.exe`)。
3.  **访问**：程序启动后会自动在浏览器中打开操作界面（推荐使用 **Chrome** 或 **Edge** 浏览器）。
    *   *手动地址：`http://127.0.0.1:5000`*

### 2. 软件激活
1.  点击主界面右上角的 **“授权”**（或“试用版”）按钮，弹出授权页面。
2.  输入您获取的合法激活码（卡密）。
3.  点击 **“立即验证”**。
    *   **温馨提示**：首次激活验证需要保持电脑网络连接。验证通过后，软件后续即可在**100%完全无网隔离的离线环境**中运行。

---

## 📖 三、 黄金五步剪片指南 (User Guide)

界面底部的五个彩色按钮对应了最标准的工业剪辑流，只需按顺序点击即可完成剪辑：

```text
  [ 1. 导入文件 ] ──> [ 2. ASR 识别 ] ──> [ 3. DeepSeek 筛选 ] ──> [ 4. 拼接视频 ] ──> [ 5. 导出文件 ]
```

### 步骤 1：导入文件 (蓝色按钮)
*   **操作**：直接将本地视频文件**拖入**左侧“编辑前视频文件”区域，或点击底部的 **“导入文件”** 按钮在电脑中选择目标视频。
*   **状态**：双击文件列表中的视频可将其加载至左侧预览播放器中。

### 步骤 2：ASR 识别 (黄色按钮)
*   **操作**：点击底部的 **“ASR 识别”**。
*   **效果**：系统在后台多线程运行识别。识别完成后，右侧的 **“ASR 字幕工作区”** 会实时展示一句句的 **“字幕磁贴”**。
*   **手动微调**：
    *   **保留/丢弃**：绿灯点亮代表该句保留，灰灯熄灭代表丢弃。
    *   **单句试听**：点击磁贴可以直接在左侧播放器中精确定位并试听该单句。
    *   **文字编辑**：点击磁贴上方的 **“编辑”** 按钮可以微调修改错别字。

### 步骤 3：DeepSeek 筛选 (红色按钮)
*   **操作**：点击底部的 **“DeepSeek 筛选”**。
*   **效果**：在弹出的浮窗中配置高分词汇、低分词汇和期望总时长，点击开始。AI 会自动选出优质片段并点亮，将其填入底部的 **“DeepSeek 筛选工作区（导出队列）”**。
*   **手动排序**：在底部的筛选工作区，您可以直接用鼠标拖动磁贴左右移动来调整视频句子的先后拼接顺序。也可以点击“导入”直接手动输入序号（如 `1, 3, 5, 12`）来进行批量挑选。

### 步骤 4：拼接视频 (绿色按钮)
*   **操作**：在右上角 **“配置”** 中，勾选您需要的效果（如“字幕烧录”、“人脸自动追踪”等），然后点击底部的 **“拼接视频”** 按钮。
*   **效果**：系统开始高速合成。合成完毕后，在左侧播放器上方切换至 **“编辑后”** 选项，即可直接试听和预览合成后的最终成品。

### 步骤 5：导出文件 (紫色按钮)
*   **操作**：点击底部的 **“导出文件”** 按钮。
*   **效果**：软件将成品视频一键复制到您在“配置”中设定的输出目录中（默认保存路径为：`C:/autocut_out`）。

---

## ⚠️ 四、 注意事项与常见问题 FAQ

> [!WARNING]
> **问题 1：导入文件后，“ASR 识别”按钮为灰色？**
> * **解答**：这是因为视频还没有被加载。请双击左侧“编辑前视频文件”列表中的视频文件，等播放器成功加载视频后，ASR 识别按钮会自动解锁。

> [!TIP]
> **问题 2：GPU 版显卡加速无法启动，或提示 DLL 缺失？**
> * **解答**：点击右上角 **“ASR”** 按钮打开侧边栏，选择“GPU模式”。如果检测到硬件环境不匹配或缺失库文件，前台会弹出紫色警报，点击 **“环境自愈”** 即可一键自动修复。

> [!NOTE]
> **问题 3：软件如何添加背景音乐（BGM）或音效？**
> * **解答**：为保证极致的离线运行速度与极简操作，**本软件专注于人声语义分段与硬核粗剪，系统本身不带背景音乐合成功能**。建议在导出视频后，将视频导入专业剪辑软件（如剪映、PR）中进行添加BGM、贴纸等后期精修和包装。

> [!CAUTION]
> **问题 4：文件夹清理**
> * **解答**：导出的视频及中间件保存在软件工作目录下，请勿在任务运行过程中手动移动或删除 `workdir` 文件夹，以免发生数据丢失。

---
*© 2026 AutoCut 智能影像工作室 - 让创作更简单*
