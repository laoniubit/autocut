# AutoCut 软件授权流程文档

> 版本：v9.5.1 | 更新日期：2026-04-16

---

## 一、核心设计原则

| 原则 | 说明 |
|---|---|
| **零网络噪音** | 仅在页面刷新时触发一次联网验证，其余所有场景均读本地证书 |
| **离线优先** | 合成、面板展示、激活后刷新均使用离线证书，不阻塞用户操作 |
| **信息隔离** | HTTP 403 的具体拒绝原因仅写入 `debug.txt`，UI 只显示"授权阻断" |
| **签名保护** | 本地证书使用 RSA 公钥（PKCS1v15 / SHA-256）验签，防止伪造与篡改 |

---

## 二、硬件指纹（HWID）

```
uuid.getnode()  →  MAC 地址原始值
    │
    └─ SHA256 截取前 32 位  →  HWID（稳定、不可逆）
```

- 写入所有对服务器的请求体 `{"hwid": "..."}`
- 写入离线证书，作为绑定当前机器的凭证

---

## 三、联网验证（`verify_license`）

**触发时机：页面刷新（`DOMContentLoaded`）或主动激活（输入激活码）**

```
verify_license(cd_key=None)
    │
    ├─ [快速返回] 内存缓存命中？(is_authorized=True 且距上次 < 600s)
    │    └─ 直接返回缓存结果，不发请求
    │
    └─ 发送 HTTP POST → abc.nelsoncode.com/wp-json/autocut/v5/auth
         请求体：{"hwid": "...", "cd_key": "...（可选）"}
         超时：10 秒
         │
         ├─ HTTP 403  →  读取响应体，提取 reason
         │               返回：False, "授权阻断: {reason}", "BLOCKED"
         │
         ├─ 其他 HTTP 错误  →  False, "API 拒绝 ({code})", "SERVER_REJECT"
         │
         ├─ JSON 解析失败  →  False, "服务器响应格式错误", "JSON_ERR"
         │
         ├─ 验签成功
         │    ├─ state=SUCCESS  →  True,  "授权验证通过 (商业版 - 有效至: {date})"
         │    ├─ state=TRIAL    →  True,  "授权验证通过 (试用版 - 剩余 N 天)"
         │    ├─ state=EXPIRED  →  False, "授权已过期 ({date})"
         │    └─ 写入 offline_license.json，更新内存缓存
         │
         ├─ 验签失败  →  False, "签名校验失败 (Protocol Mismatch)", "SIG_ERR"
         │
         └─ 网络异常/超时  →  进入离线降级（见下节）
```

### 验签协议（v5.0）

服务器签名的原始载荷格式：

```
{state}|{hwid}|{timestamp}
```

客户端使用内置 RSA 公钥验证，任何字段被篡改均导致验签失败。

---

## 四、离线验证（`verify_license_offline`）

**触发时机：打开授权面板、合成视频前、激活成功后刷新**

```
verify_license_offline()
    │
    ├─ [快速返回] 内存缓存命中？(is_authorized=True 且距上次 < 600s)
    │    └─ 直接返回缓存结果，不读磁盘
    │
    ├─ offline_license.json 不存在？
    │    └─ False, "未找到本地授权证书，请联网验证一次", "NO_CERT"
    │
    ├─ 读取 offline_license.json
    │    ├─ 验签失败  →  False, "本地证书签名无效", "SIG_ERR"
    │    │
    │    ├─ expire_date < 当前时间  →  False, "授权已于 {date} 过期", "EXPIRED"
    │    │
    │    ├─ state=EXPIRED  →  False, "授权已过期", "EXPIRED"
    │    │
    │    ├─ state=SUCCESS  →  True, "授权验证通过 (商业版 - 离线模式)"
    │    └─ state=TRIAL   →  True, "授权验证通过 (试用版 - 离线模式)"
    │
    └─ 读取异常  →  False, "证书读取失败: {ExceptionType}", "CERT_ERR"
```

> **离线过期防线**：到期判断使用本地系统时间对比证书中的 `expire_date` 字段。
> 注意：该机制依赖本地系统时间，不防时间回拨。

---

## 五、触发时机全局映射

| 场景 | 调用方式 | 发网络请求 |
|---|---|---|
| **页面刷新 / 冷启动** | `verify_license()` (联网) | ✅ 是（唯一时机） |
| **主动输入激活码** | `verify_license(cd_key)` (联网) | ✅ 是 |
| **打开授权状态面板** | `verify_license_offline()` | ❌ 否 |
| **激活成功后刷新面板** | `verify_license_offline()` | ❌ 否（激活已更新证书） |
| **点击合成视频前** | `verify_license_offline()` | ❌ 否 |
| ~~5 分钟定时轮询~~ | ~~已删除~~ | ~~❌~~ |

---

## 六、内存缓存机制

```
_cache_authorized  # 上次验证是否通过
_cache_msg         # 上次验证的消息文本
_cache_state       # 上次验证的状态码
_cache_time        # 上次验证的时间戳 (Unix)
```

- 缓存有效期：**600 秒（10 分钟）**
- 联网验证和离线验证**共享同一份缓存**
- 缓存命中时，`verify_license` 和 `verify_license_offline` 均直接返回，**不发请求、不读磁盘**

---

## 七、本地证书文件（`offline_license.json`）

| 字段 | 类型 | 说明 |
|---|---|---|
| `state` | string | `SUCCESS` / `TRIAL` / `EXPIRED` |
| `hwid` | string | 机器硬件指纹（用于验签） |
| `timestamp` | string | 服务器签名时间戳 |
| `expire_date` | string | 授权到期日期，格式 `YYYY-MM-DD HH:MM:SS` |
| `days_left` | int | 试用版剩余天数 |
| `signature` | string | Base64 编码的 RSA 签名 |

- 文件路径：`{BASE_DIR}/offline_license.json`
- 写入时机：联网验证成功后自动写入/覆盖
- 读取时机：离线降级 / `verify_license_offline()`

---

## 八、UI 信息隔离规则

```
d.message (后端完整信息)
    │
    ├─ 写入 debug.txt（[License Audit] 前缀，LICENSE_AUDIT 级别）
    │
    └─ 显示至 UI
         ├─ 若以 "授权阻断:" 开头  →  UI 只显示 "授权阻断"（具体原因隐藏）
         └─ 其他情况               →  正常显示完整消息
```

---

## 九、合成阻断流程

```
用户点击"合成视频"
    │
    └─ start_batch (SocketIO)
         │
         └─ [mode == 'complex'] verify_license_offline()
              ├─ True  →  继续执行合成管线
              └─ False →  阻断原因静默写 debug.txt
                          广播错误日志到 UI（通用措辞，不暴露具体原因）
                          emit 'done' → "Synthesis Denied"
```

---

## 十、相关文件索引

| 文件 | 职责 |
|---|---|
| `acut_license.py` | 核心鉴权逻辑：HWID、联网验证、离线验证、签名校验 |
| `acut_server.py` | Flask 端点：`/api/license_status`、`/api/license_status_offline`、`/api/license_activate`；合成前鉴权 |
| `templates/index.html` | 前端：`checkLicenseStatus(offline)`、`showLicenseModal`、`activateLicense` |
| `offline_license.json` | 本地授权证书缓存文件 |
| `debug.txt` | 完整授权审计日志（含 403 具体原因） |
