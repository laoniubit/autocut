# AutoCut Matrix AI - Mac 依赖清单
# 使用方法: pip install -r requirements_mac.txt
#
# 注意: Mac 版不使用 CUDA，不需要 torch/ctranslate2 等 GPU 包。
# onnxruntime 使用 CPU 版本（Mac ARM 支持良好）。

# --- 镜像源 (可选，中国大陆加速) ---
# -i https://pypi.tuna.tsinghua.edu.cn/simple

# --- 核心 Web 框架 ---
flask>=2.3.2
flask-socketio>=5.3.4

# --- 系统工具 ---
psutil>=5.9.5

# --- 授权与加密 ---
cryptography>=41.0.3

# --- 网络请求 ---
requests>=2.31.0

# --- AI 推理 (纯 CPU, 跨平台) ---
numpy>=1.24.0
# Mac ARM64 专用 ONNX Runtime (CoreML 加速可选)
onnxruntime>=1.17.0

# --- 视觉处理 ---
# mediapipe Mac ARM64 从 0.10.9 开始支持 Apple Silicon
mediapipe>=0.10.9
opencv-python>=4.8.0

# --- 打包工具 ---
pyinstaller>=6.3.0
