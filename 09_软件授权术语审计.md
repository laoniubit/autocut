# AutoCut 授权管理话术盘点

根据代码库的检索结果，以下是目前系统中涉及"软件授权管理"的所有前端 UI 显示话术以及后端相关状态信息：

---

## 一、前端 UI 面板 (`templates/index.html`)

### 面板与模块标题

| 位置 | 话术 |
|------|------|
| 抽屉面板标题 | `软件授权管理` |
| 状态卡片标题 | `授权状态` |
| 机器码卡片 | `设备识别码` |
| 级别卡片 | `授权级别` |
| 输入框卡片 | `激活码` |
| 底部说明 | `💡 授权服务说明` |

### 动态状态话术（JS 动态渲染）

| 场景 | 状态标题 (`license-detail-title`) | 级别 (`license-level`) |
|------|----------------------------------|------------------------|
| 初始占位 | `授权状态` | `未激活` |
| 试用中 (`TRIAL`) | `试用体验中` | `免费体验` |
| 已激活 (正式版) | `正式版授权已激活` | `商业授权` |
| 过期 (`EXPIRED`) | `授权已过期或受限` | `已过期` |
| 未授权 | `授权已过期或受限` | `未授权` |

### 激活码输入区

- **占位符**：`XXXX-XXXX-XXXX-XXXX`
- **默认提示文案**：`输入激活码以解锁全部功能`
- **激活按钮**：`立即激活`

### 头部徽章（header badge）

- **初始状态**：`未激活`

---

## 二、授权服务说明文案（底部静态文案）

```
• 软件采用机器码绑定模式，一机一码。
• 激活码一经核销，该设备将获得正式版使用权限。
• 如需更换电脑，请联系客服进行解绑迁移。
```

---

## 三、后端验证日志（`acut_license.py`）

### 授权有效状态

| 场景 | 日志话术 |
|------|---------|
| 在线验证 - 商业版 | `授权验证通过 (商业版 - 有效至: {日期})` |
| 在线验证 - 试用版 | `授权验证通过 (试用版 - 剩余 {N} 天)` |
| 在线验证 - 通用 | `授权验证通过 (Online)` |
| 离线验证 - 商业版 | `授权验证通过 (商业版 - 离线模式)` |
| 离线验证 - 试用版 | `授权验证通过 (试用版 - 离线模式)` |

### 授权失效/异常

| 场景 | 日志话术 |
|------|---------|
| 在线检测 - 过期 | `授权已过期 ({日期})` |
| 离线检测 - 过期 | `授权已于 {日期} 过期` |
| 离线检测 - 通用 | `授权已过期` |

---

## 四、后端接口拦截（`acut_server.py`）

- API 校验失败时返回 HTTP 403，附带信息：`授权阻断: {具体原因}`
