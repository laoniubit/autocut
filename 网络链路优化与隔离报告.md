# AutoCut 工业级管线公网链路优化与隔离报告

**审计时间**：2026-05-19
**核心目标**：在保障核心业务流转的前提下，清除冗余的网络探针并实施国内网络加速优化，使系统兼顾“物理隔离友好”与“国内网络极速响应”。

---

## 一、 保留的白名单（核心业务链路）

根据安全审计与业务需要，系统在前后端强制保留了以下公网请求链路，确保产品的商业闭环与基础模型能力可用：

1. **`abc.nelsoncode.com`**：用于软件正版授权的合法性下发与心跳防伪验证，是项目运行的基础鉴权通道（位于 `acut_license.py` 等）。
2. **`cut.xmbs.top`**：用于 UI 界面上“获取激活码”按钮的业务转化跳转（位于 `index.html`）。
3. **`api.deepseek.com`**：AI 语义过滤与处理面板保留的预设默认值。允许具有公网条件的用户无缝接入顶尖的 DeepSeek 官方大语言模型服务（位于 `index.html`）。

---

## 二、 基础组件网络获取优化（国内镜像源改造）

针对部署时可能发生的“墙外下载卡死、断点拉取失败”等阻塞性网络灾难，对基础底层组件进行了流量重定向：

1. **后端 Whisper 核心资产拉取 (`acut_server.py`)**：
   - 将原先直连 HuggingFace 官方源（`huggingface.co`）的代码进行覆写。
   - 现全部统一跳转至中国境内稳定高速镜像站 **`hf-mirror.com`**，彻底修复模型通过 Web UI 无法顺利下载的问题，并配有前端下载进度与中文日志回显。
2. **前端 WebAssembly 分词引擎 (`transformers.min.js`)**：
   - 底层运行时配置 `remoteHost` 被从官方的 `https://huggingface.co/` 硬编码为 `https://hf-mirror.com/`，且保持了 `allowRemoteModels:!0`。
   - 即使前端在离线资源不齐备时临时向云端拉取必要的分词器权重，也能做到秒级响应。

---

## 三、 绝对冗余探针移除（深层隔离净化）

为确保项目部署在严苛内网（无外网）环境时，不产生由超时等待导致的“假死”或“长时间白屏”，移除了无用的三方调用：

- **静态字体依赖 (`RELEASE_INSTALL_GUIDE.html`)**：
   - 物理删除了对 Google Fonts (`fonts.googleapis.com`) 的外部样式加载。由于该域名在无外网/国内部分地区极易阻断，剥离它能保障发布手册在内网机的绝对秒开体验。

---

## 结论

当前系统已达成最佳网络状态：
1. **纯粹性**：斩断了无关的三方跟踪与界面加载依赖。
2. **敏捷性**：关键开源 AI 资产的拉取全面倒向高性能的国内镜像节点。
3. **隔离就绪**：若将包含预置 `models/` 权重的包交付至断网工控机，系统亦可实现业务链路降级闭环，而不会在任何前端加载和后端推理环节引发 Crash。
