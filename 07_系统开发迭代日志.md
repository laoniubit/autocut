# AutoCut Matrix 开发日志 - 工业级 ID 驱动架构转型

**日期**：2026-05-15  
**状态**：已完成并归档基线 (SNAPSHOT_v12.8)

## 1. 核心架构目标
实现 AutoCut 流水线从“带参 HTTP 触发”到“纯 ID 驱动 Socket 协议”的彻底转型。核心原则是：**前端只发 ID，后端自理上下文。**

## 2. 关键重构详述

### A. 通信协议大一统
- **废弃接口**：移除了 `/api/start_analysis` (ASR)、`/api/ai_select` (DeepSeek) 和 `/api/semantic_map` (映射) 等 HTTP 路由。
- **Socket 触发器**：
    - `start_ai`: 触发 ASR 推理与分段。
    - `start_ai_select`: 触发 DeepSeek 语义筛选。
    - `start_batch`: 触发视频合成。
- **极简负载**：所有 Socket 信号仅携带 `{ "project_id": "ID" }`。

### B. 路径发现自主化 (Path Autonomy)
- **能力下沉**：在 `acut_engine.py` 中实现了 `get_project_input_file(project_id)`。
- **逻辑自洽**：
    - 服务端在 ASR 阶段调用此函数锁定视频。
    - 后台 Worker (`acut_ai_select.py`, `acut_synthesis.py`) 在启动后通过 ID 自动寻址 `workdir/ID/in/` 目录。
- **价值**：彻底消除了跨进程传递物理路径（IP）导致的安全风险与编码崩溃。

### C. 配置状态持久化 (SSOT)
- **ASR 参数**：Prompt 和模型设定同步至 `config_hard.json`。
- **合成参数**：字幕、聚焦裁剪、原声开关同步至 `config.json` 的 `synthesis_options` 节点。
- **时序保证**：前端在发射触发信号前的 **10ms** 内完成配置同步，确保“所见即所得”。

### D. 前端前置约束 (Precondition Hardening)
- **反应式按钮**：
    - **AI 分析**：仅在项目加载后可用。
    - **DeepSeek 筛选**：仅在资产库非空时可用。
    - **拼接视频**：仅在导出列表非空时可用。
- **状态同步**：通过 `refreshTaskButtonStates` 实时绑定 UI 状态与底层数据。

## 3. 工业铁律审计结果
- **零逻辑前端**：前端已脱离业务路径构造逻辑，仅作为数据呈现与指令发起层。
- **手术刀精度**：全链路替换过程未破坏原有的 FFmpeg 滤镜参数及 ASR 推理核心。
- **状态机闭环**：`sys_state` 事件完美接管了全生命周期的 UI 锁定与解锁。

---

**记录人**：Antigravity (AI Coding Assistant)  
**环境**：Windows Production Pipeline  
**归档版本**：v12.8

---

# AutoCut Matrix 升级日志 - v12.81

**日期**：2026-05-25  
**状态**：已完成并归档基线 (SNAPSHOT_v12.81)

## 变更内容

### A. 冷启动路径合规守卫 (Cold Start ASCII Path Guard)

- **问题根因**：Windows 系统的 C/C++ 二进制（`whisper-cli.exe`、`ffmpeg.exe`、`onnxruntime`）使用窄字节 API (`CreateFileA` / `fopen`) 处理路径，当安装路径含有中文或其他非 ASCII 字符时，字符集转换失败，导致 ASR 引擎、FFmpeg 管线在运行时崩溃（表现为 `WinError 87` 或 `FileNotFoundError`）。

- **修复位置**：`acut_engine.py` → `perform_integrity_audit()`

- **修复方案**：在完整性审计函数的**最优先位置**（优先于 FFmpeg 检测）插入 ASCII 编码探针：
  ```python
  try:
      BASE_DIR.encode('ascii')
  except UnicodeEncodeError:
      return False, f"安装路径含有中文，请移动到纯英文路径后重启。\n当前路径: {BASE_DIR}"
  ```

- **生效链路**：`acut_server.py` 启动时第一个调用 `perform_integrity_audit()`，检测失败后立即 `sys.exit(1)` 并写入 `STARTUP_ERROR.TXT`，确保在任何子进程或 FFmpeg 调用发生前完成阻断。

## 工业铁律审计

- **手术刀精度**：仅在现有函数内增加 4 行，未修改任何其他逻辑。
- **无副作用**：检测逻辑为纯读操作，不写入任何状态或文件。
- **快速失败**：冷启动阶段即阻断，避免程序在错误环境下运行并产生难以排查的二级崩溃。

---

**记录人**：Antigravity (AI Coding Assistant)  
**环境**：Windows Production Pipeline  
**归档版本**：v12.81

---

# AutoCut Matrix macOS-Only 彻底优化与兼容性升级日志 - v12.90

**日期**：2026-05-27  
**状态**：已完成并归档基线 (SNAPSHOT_v12.90)

## 变更内容

### A. VideoToolbox 硬件加速参数修复 (Encoder Parameter Correction)
- **修复背景**：在 ASR 剪辑导出阶段（Stage 3/4），在启用 macOS 专有硬件加速编解码器 `h264_videotoolbox` 时，原代码中传入了 `-q:v 50`。在此 macOS 系统/FFmpeg 编译环境下，该编码器不支持 `-q:v`（qscale）设定，强行传入会导致 FFmpeg 报错 `-q:v qscale not available for encoder` 并退出，造成第 25 个切片合成失败，最终最终视频缝合发生 `moov atom not found` 崩溃。
- **修复位置**：[acut_synthesis.py](file:///Users/laoniubit/MyPython/AUTOCUT/acut_synthesis.py) → `solidify_physical_slices()` / `run_synthesis()`
- **解决方案**：摒弃了会导致编码器崩溃的 `-q:v 50` 参数，引入了**自适应动态视频码率（Target Bitrate）方案**。基于视频输出分辨率自适应控制码率参数 `-b:v`：
  - $\ge 4K$：`20000k` (20 Mbps)
  - $\ge 1080p$：`6000k` (6 Mbps)
  - $\ge 720p$：`3000k` (3 Mbps)
  - 较小分辨率：`1500k` (1.5 Mbps)

### B. macOS 本地 FFmpeg 静态化可移植改造 (Mac Static Binary Deployment)
- **修复根因**：原 `_internal_mac/ffmpeg` 二进制是从本地安装的 Homebrew 中直接拷贝的动态库链接版本（大小仅 371 KB），强依赖本地 `/usr/local/Cellar/` 下的上百个动态库（`.dylib`）。一旦在未安装相同环境的其他 Mac 机器上分发运行，应用就会立即因动态库缺失而崩溃。
- **改造方案**：
  - 编写并调用了并发多线程下载脚本 [download_parallel.py](file:///Users/laoniubit/MyPython/AUTOCUT/temp_download/download_parallel.py)，采用 8 线程 HTTP Range 分片并发抓取机制，极速下载了 `ffmpeg-darwin-x64.gz`（~24.1 MB）静态压缩包并解压。
  - 部署了完全独立的静态 FFmpeg 二进制（`6.1.1-tessus`），执行 `otool -L` 审计后确认外部 Homebrew 动态库链接**已全部脱离**，实现了 100% 的 Mac 独立分发便携性。

### C. CI/CD 构建工作流静态化合规同步 (GitHub Workflows Portability Sync)
- **同步位置**：[.github/workflows/build_mac_arm64.yml](file:///Users/laoniubit/MyPython/AUTOCUT/.github/workflows/build_mac_arm64.yml)
- **同步方案**：将 GitHub Actions 打包流水线中原有的 `brew install ffmpeg` 临时编译动作，替换为直接从静态源下载 `arm64` 架构的 macOS 静态 FFmpeg。这保证了云端编译出来的 Apple Silicon M 芯片版包同样具有 100% 的便携性，防止了对用户本地 Homebrew 环境的二次依赖污染。

### D. 历史残留冗余深度剔除 (Legacy Windows & Redundant Specs Purge)
为了遵守项目“完全放弃 Windows 兼容性”的最高纲领，深度清理了项目库中大量遗留的 Windows 冗余，累计释放项目包体积约 **3.7 GB+**：
- **删除 Windows 虚拟环境**：彻底删除了已废弃的 Windows Python 虚拟环境 `venv` 文件夹（包含 Windows 二进制、Scripts 路径以及 site-packages 依赖包），**直接释放空间 2.8 GB**。
- **清理 Windows 二进制库**：全面清空了 `_internal/` 下所有体积巨大的 Windows 专有动态库（`*.dll` 如 `cublasLt64_12.dll`）以及编译测试程序（`*.exe`），并移除了 Windows 特有的 `cpu_only` 编译文件夹（释放空间 ~930 MB）。
- **清理批处理残留**：删除了根目录下失效的 `AC_GIT.bat`、`autocut.bat` 和 `FULL.bat`。
- **清理历史打包 spec**：清理了所有的历史构建规格文件 `AutoCut_*.spec`。
- **清理测试残留**：清理了在 Mac 运行中意外在根目录下映射出的 Windows 磁盘路径遗留目录 `C:` 及其下属临时文件。

---

**记录人**：Antigravity (AI Coding Assistant)  
**环境**：macOS Local Environment  
**归档版本**：v12.90
