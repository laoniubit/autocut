# AutoCut Matrix AI 架构分析报告 (v9.5)

## 1. 核心架构概述
本项目采用 **“后端引擎 + 独立 Worker + WebUI”** 的工业级解耦架构。核心逻辑由 Python 编写，通过 Flask-SocketIO 与前端交互，复杂的 AI 任务通过独立的进程（Worker）执行，以确保主进程的响应能力和内存稳定性。

---

## 2. 模块功能分类

### 2.1 核心调度层 (Core & Server)
*   **[acut_server.py](file:///e:/autocut/acut_server.py)**: 系统大脑。负责 Flask 路由、SocketIO 状态广播、任务队列管理以及硬件状态监控。
*   **[acut_engine.py](file:///e:/autocut/acut_engine.py)**: 底层驱动。负责路径管理、配置持久化（config.json）、FFmpeg 配置以及物理文件句柄的安全释放。

### 2.2 AI 任务流 (Worker Layer)
任务层采用 **“参数化注入 (Parameter Injection)”** 模式，通过 CLI 参数实现进程级隔离与状态对齐：
*   **[acut_ai_scan.py](file:///e:/autocut/acut_ai_scan.py)**: 统一分析器。执行音频提取、ASR 识别、语义映射及特征提取。
*   **[acut_ai_select.py](file:///e:/autocut/acut_ai_select.py)**: 智能筛选器。基于语义索引和用户偏好进行片段选取。
*   **[acut_synthesis.py](file:///e:/autocut/acut_synthesis.py)**: 物理合成器。执行视频分割、对齐、字幕压制及最终拼接。

### 2.3 AI 引擎与逻辑 (Service Layer)
*   **[acut_asr.py](file:///e:/autocut/acut_asr.py)**: Faster-Whisper 封装。负责模型加载、卸载、推理进度跟踪及硬件加速校验。
*   **[acut_deepseek.py](file:///e:/autocut/acut_deepseek.py)**: LLM 语义增强。处理字幕的二次清洗与高层语义理解。
*   **[acut_zoom.py](file:///e:/autocut/acut_zoom.py)**: 视觉增强。实现基于 AI 的动态缩放与画幅优化。

### 2.4 安全与辅助 (Security & Utils)
*   **[acut_license.py](file:///e:/autocut/acut_license.py)**: 离线授权引擎。支持设备指纹核验与离线证书管理。
*   **[acut_backup.py](file:///e:/autocut/acut_backup.py)**: 物理自愈。负责任务失败后的状态恢复与文件备份。

---

## 3. 业务数据流 (Workflow)
1.  **导入阶段**：`acut_server.py` 接收文件，在 `workdir/` 下创建项目目录及 `in/`, `temp/`, `out/` 结构。
2.  **分析阶段**：启动 `acut_ai_scan.py` 进行 ASR 识别，生成 `semantic_index.json` 及预览缩略图。
3.  **决策阶段**：用户手动选择或启动 `acut_ai_select.py` 自动筛选，更新 `synthesis_queue.json`。
4.  **合成阶段**：启动 `acut_synthesis.py` 调用 FFmpeg 进行物理切片与合成，输出至 `out/`。

---

## 4. 目录规范 (Physical Structure)
*   **`workdir/`**: 业务数据目录，按 `project_id` 分层。
*   **`_internal_/`**: 运行时日志与关键配置（`debug.txt`, `config.json`）。
*   **`models/`**: 离线 AI 模型权重库库（faster-whisper 等）。

---

## 5. 工业级稳定性设计点
1.  **显式资源管理**：摒弃物理强制中止（os._exit），采用 `try...finally` 闭环结合双重 `gc.collect()`，确保 AI 模型在子进程退出前 100% 显式回收硬件资源。
2.  **进程级隔离**：AI 推理不占用主 Flask 进程内存，通过独立 Worker 运行，利用子进程天然的地址空间隔离保障系统鲁棒性。
3.  **原子化 I/O**：通过 `save_json_atomic` 确保断电时配置文件不损坏。
4.  **环境自检**：启动时校验 FFmpeg 等核心依赖，失败则写入 `STARTUP_ERROR.TXT` 并退出。
