# DeepSeek 工业级视频精选方案 (v9.0 Dual-Axis Matrix)

## 1. 架构总览
本方案采用 **“物理过滤 + AI 导演 + 后端精算”** 的混合架构，旨在解决大语言模型（LLM）在长序列视频筛选中的“时长幻觉”与“逻辑混乱”问题。

---

## 2. 核心模块详解

### 2.1 物理层：红线拦截器 (Physical Pre-filter)
*   **机制**: 后端 Python 在数据进入 AI 前进行关键词硬匹配。
*   **关键词库**: 包含“链接、下单、券、米、块钱、点关注、粉丝团、快递”等 30+ 直播废词。
*   **作用**: 物理降噪，减少 30%-50% 的无效 Token，确保 AI 算力聚焦于核心语义。

### 2.2 逻辑层：高密度输入 (High-Density Input)
*   **格式**: 采用 `ID | 内容` 极简文本格式。
*   **目的**: 消除 JSON 语法干扰，降低 AI 解析疲劳，有效控制幻觉。

### 2.3 语义层：双轴矩阵标注 (Dual-Axis Matrix)
AI 作为“导演”和“精算师”进行多维标注：
*   **维度一：功能标签 (Category)**
    *   `H (Hook)`: 痛点/勾子（开头）。
    *   `S (Selling Point)`: 核心卖点（骨架）。
    *   `D (Detail)`: 穿搭/细节（肌肉）。
    *   `E (Ending)`: 金句/总结（结尾）。
*   **维度二：价值评分 (Score: 0-10)**
    *   用于同一类别内部的优先级排名，决定在时长受限时的留存顺位。

### 2.4 算力层：故事线重组器 (Narrative Aggregator)
由后端 Python 执行的确定性算法：
*   **必保逻辑**: 优先确保所有高分的 `H` 和 `E` 入选。
*   **类内择优**: 在 `S` 和 `D` 数量超标时，根据 `Score` 进行末位淘汰。
*   **叙事重排**: 强行打破原视频时序，按 **`[H组] -> [S组] -> [D组] -> [E组]`** 物理重组片段。
*   **时长锁死**: Python 实时累加物理时长，一旦达到 Target Limit 立即截断。

---

## 3. 技术优势 (Industrial Strengths)
*   **零时长幻觉**: AI 不参与数学计算，计时主权完全回归后端。
*   **逻辑结构化**: 保证视频具有强勾子开头、饱满中段和有力结尾。
*   **高性能**: 支持 200+ 片段的长视频流处理，响应稳定，遵循度高。

---

## 4. 维护说明
*   **核心代码**: `acut_deepseek.py`
*   **日志位置**: `workdir/<pid>/temp/deepseek_dual_axis.txt`
*   **版本控制**: v9.0 (Dual-Axis Matrix Mode)
